version: '2'

networks:
  distribuida:
    external:
      name: fmedel_network

services:
  web:
    build:
      context: .
      dockerfile: api.dockerfile
    #command: npm start
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
    ports:
      - "8085:8085"
#    environment:
